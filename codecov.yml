codecov:
  notify:
    after_n_builds: 2
  ignore:
    - "Tests/**/*" # ignore Tests

coverage:
  status:
    project:
      default: off
      unittest:
        flags: unittest
        target: 95%

flags:
  unittest:
    # only test unittests
    paths:
      - Tests/
